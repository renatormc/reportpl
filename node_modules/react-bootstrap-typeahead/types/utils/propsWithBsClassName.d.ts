import type { Size } from '../types';
interface Props {
    className?: string;
    isInvalid?: boolean;
    isValid?: boolean;
    size?: Size;
}
export default function propsWithBsClassName<T>({ className, isInvalid, isValid, size, ...props }: Props & T): Omit<Props & T, "className" | "isInvalid" | "isValid" | "size"> & {
    className: string;
};
export {};
